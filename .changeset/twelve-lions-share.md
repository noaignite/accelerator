---
'@noaignite/react-utils': minor
---

Add a `disableSuspense` option to `createRenderBlock` so block rendering can conditionally skip the `Suspense` wrapper based on a boolean or context-aware callback.
