---
'@noaignite/react-utils': minor
---

Add a `disableErrorBoundary` option to `createRenderBlock` so block rendering can conditionally skip the `ErrorBoundary` wrapper based on a boolean or context-aware callback.
