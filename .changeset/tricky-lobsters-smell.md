---
'@noaignite/react-centra-checkout': minor
---

Add typed event tracking for checkout handlers and hooks via `CentraEvents`, and export event types publicly so apps can track handler calls in a type-safe way.
