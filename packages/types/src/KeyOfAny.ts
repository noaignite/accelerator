/**
 * Type that represents the possible values to be considered keys.
 *
 * @deprecated Use the built-in type `PropertyKey` instead.
 * @todo Remove in v2.0.0
 *
 * @example
 * ```ts
 * Record<KeyOfAny, unknown>
 * ```
 */
export type KeyOfAny = keyof any
